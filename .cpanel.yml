---
deployment:
  tasks:
    - export DEPLOYPATH=/home/lerolero/mocktb.lerolero.com.ec
    #clear the deploy path
    - rm -rf $DEPLOYPATH/*
    #copy the files to the deploy path
    - /bin/cp -R * $DEPLOYPATH
